class CaseLimited{
    constructor(eventId, VIN, firstName, lastName)    {
        this.eventId = eventId;
        this.VIN = VIN;
        this.firstName = firstName;
        this.lastName = lastName;
    }
}

module.exports = CaseLimited;